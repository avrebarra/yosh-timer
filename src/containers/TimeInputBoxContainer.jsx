import React from 'react';
import { BrowserRouter } from 'react-router-dom'
import TimeInputBox from 'components/TimeInputBox'

const TimeInputBoxContainer = (props) => (
  <TimeInputBox/>
);

export default TimeInputBoxContainer;
